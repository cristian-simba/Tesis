import * as React from 'react';
import { Dialog, DialogTrigger, DialogContent } from '@radix-ui/react-dialog';

function Dashboard() {
  return (
    <div>
      <h1>Dashboard</h1>
      {/* <Dialog>
        <DialogTrigger>Open Drawer</DialogTrigger>
        <DialogContent style={{ position: 'absolute', width: '250px', left: 0, top: 0, height: '100%' }}>
          <h2>Drawer Content</h2>
          <p>This is the content inside the drawer.</p>
        </DialogContent>
      </Dialog> */}
    </div>
  );
}

export default Dashboard;